# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
node_modules/
jspm_packages/

# Optional npm cache directory
.npm

# Output directories
dist/
build/
.next/

# Environmental variables — NEVER commit keys
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Operating System Files
.DS_Store
Thumbs.db

# ── RAG Pipeline Generated Data (local only, never commit) ───
# LanceDB vector index — large binary files, rebuilt by: npm run index
backend/data/lancedb/
backend/data/hashes/
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal

# Legacy vector store
backend/vector_store.json

# ── Local repo clones (client code — never commit) ───────────
repo/
testingrepo/

# ── Bundler output ────────────────────────────────────────────
dist/
*.exe
*.pkg

# ── Tunnel binaries ───────────────────────────────────────────
cloudflared.exe

